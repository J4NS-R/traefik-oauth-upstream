http:
  middlewares:
    oauthup:
      plugin:
        traefik-oauth-upstream:
          clientId: "clientId"
          clientSecret: "clientSecret"
    demomid:
      plugin:
        demoplugin:
          headers:
            Foo: Bar
